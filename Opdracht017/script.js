// const hundredMore = function (number) {
// const sum = number > 100;
// return sum;
// };

// const age = hundredMore(100);
// console.log(age);

const enterDecision = function (maxClub, inClub, enterAge) {
    const number = ("enterAge >= 18");
    const sum = maxClub + inClub;
    if (sum > 100) {
        return "It's too busy now, come back later";
    }
    return "Come in";
};

const age(19);
const result = enterDecision(9, 6)
console.log(result);

